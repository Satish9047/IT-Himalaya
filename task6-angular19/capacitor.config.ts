import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taskManager.app',
  appName: 'task6-angular19',
  webDir: 'dist/task6-angular19/browser',
};

export default config;
